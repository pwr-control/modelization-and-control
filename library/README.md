# Library

This library contains a collection of models used along the whole repo.

**foundation**:
- collection of simscape models built in ssc;

**masked models**:
- collection of simulunk block with masked interface;

**user_defined_functions**:
- collection of ccaller functions written in C;

**setup_files**:
- collection global setting and global data;